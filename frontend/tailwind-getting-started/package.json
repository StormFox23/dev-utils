{
  "scripts": {
    "watch": "npm-watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "postcss css/tailwind.css -o dist/output.css",
    "dev": "concurrently \"npm run build && live-server src --port=8080\" \"npm run watch\""
  },
  "devDependencies": {
    "autoprefixer": "^10.4.13",
    "postcss": "^8.4.21",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.2.7"
  }
}
